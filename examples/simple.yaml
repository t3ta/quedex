version: 1
description: "Simple YAML plan example"

run:
  name: "yaml-example"
  max_concurrency: 1
  fail_fast: true

tasks:
  - id: analyze
    title: "Analyze target file"
    mode: research
    kind: codex
    codex:
      prompt: "Analyze the src/lib.rs file in the quedex project"
      model: "o3-mini"
